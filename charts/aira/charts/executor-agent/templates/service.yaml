apiVersion: v1
kind: Service
metadata:
  name: {{ include "executor-agent.fullname" . }}
  labels:
    {{- include "executor-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "executor-agent.selectorLabels" . | nindent 4 }}
